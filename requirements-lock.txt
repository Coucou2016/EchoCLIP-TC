# Pinned working environment (CPU Windows host, 2026-09).
# Prefer requirements.txt for flexible installs; use this for reproduce-ish locks.
# For --paper / official EchoCLIP hub path, prefer requirements-paper.lock
# (pins open-clip-torch; keeps transformers in the <5 range of requirements.txt).
#
# Conflict note: do not mix this file's older transformers==5.x pin with
# requirements.txt (<5). Prefer requirements-paper.lock for paper runs.
#
# Environment note:
# - Python 3.10+ recommended
# - GPU optional (torch CUDA build when available)
# - Windows _lzma / timm issues → use requirements-minimal.txt + vision_backbone=simple_cnn (demo only)
# - Official EchoCLIP / open_clip hub needed for --paper

torch==2.12.0
torchvision==0.27.0
numpy==2.4.5
Pillow==12.2.0
PyYAML==6.0.3
tqdm==4.67.1
timm==1.0.27
# Align with requirements.txt upper bound (<5); was incorrectly 5.9.0
transformers==4.44.2
opencv-python-headless==4.13.0.92

# Optional (paper init / hub) — also pinned in requirements-paper.lock
open-clip-torch==2.26.1

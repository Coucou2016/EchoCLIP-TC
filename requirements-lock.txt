# Pinned working environment (CPU Windows host, 2026-09).
# Prefer requirements.txt for flexible installs; use this for reproduce-ish locks.
# Note: do not copy machine-local path URLs from pip freeze (e.g. file:///C:/...).
#
# Environment note:
# - Python 3.10+ recommended
# - GPU optional (torch CUDA build when available)
# - Windows _lzma / timm issues → use requirements-minimal.txt + vision_backbone=simple_cnn (demo only)
# - Official EchoCLIP / open_clip hub needed for --paper (not pinned here as weight files)

torch==2.12.0
torchvision==0.27.0
numpy==2.4.5
Pillow==12.2.0
PyYAML==6.0.3
tqdm==4.67.1
timm==1.0.27
transformers==5.9.0
opencv-python-headless==4.13.0.92

# Optional (paper init / hub)
# open-clip-torch>=2.20.0
